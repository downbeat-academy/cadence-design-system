import React from 'react';
declare type Props = {
    children: React.ReactNode;
    attribution?: string;
    source?: string;
};
export declare const Blockquote: ({ children, attribution, source }: Props) => JSX.Element;
export {};
