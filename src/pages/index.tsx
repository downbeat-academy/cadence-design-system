import type { NextPage } from 'next';
import { Divider } from '../components/divider'

const Home: NextPage = () => {
	return (
		<div>
			<p>Home Page</p>
			<Divider />
		</div>
	);
};

export default Home